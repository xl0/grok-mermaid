{
  "name": "lovely-mermaid-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test",
    "check": "biome check .",
    "fix": "biome check --write .",
    "typecheck": "tsgo --noEmit -p packages/lovely-mermaid",
    "differential": "bun run tools/differential/run.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@types/bun": "latest",
    "@typescript/native-preview": "latest"
  }
}
