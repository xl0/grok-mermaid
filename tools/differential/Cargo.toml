[package]
name = "differential"
version = "0.0.0"
edition = "2024"

# Pinned to what grok-build's Cargo.lock resolves, so the golden output is the
# renderer as shipped rather than the latest release.
[dependencies]
ratatui = "0.29"
unicode-width = "=0.2.0"

[profile.release]
debug = false
